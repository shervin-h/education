enum HandSomeState {
  handThumbsUpState,
  handThumbsDownState,
  handThumbsDefaultState
}

enum ListType {
  newest,
  mostVisited,
  tagged,
  bookmarked,
  considered,
  bySource,
  byCourse,
  none,
}
